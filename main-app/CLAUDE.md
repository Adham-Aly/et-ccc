@AGENTS.md

# main-app: additional instructions

This file's `@AGENTS.md` line is generated by Next (never hand-edit `AGENTS.md`; it is re-upserted
by `next dev`, plan §8.8). Everything below is hand-written.

- **Read `../context/00-START-HERE.md` first** — it is the entry point for every agent in this
  workspace, and points at the plan, the operating rules and the current progress.
- **Call tools only through `../.tooling/bin/*`** (npm scripts: `../.tooling/bin/npm run …` from
  this directory), never bare `node`, `npm`, `npx`, `playwright`, `git`, `gh`, `vercel`. A
  PreToolUse guard hook blocks bare calls. See `../CLAUDE.md` and `../.tooling/README.md`.
- Read `../work/04-app/brief.md` in full before changing anything in this app.
