// Placeholder home page so `next build` passes and a first Vercel deploy can work (plan §12 P4
// step 2). Plain semantic markup, no styling: the design lead owns presentation (brief §3).
export default function HomePage() {
  return (
    <main>
      <h1>et-ccc</h1>
      <p>A free reading course from zero to CCC Senior, in Python 3.8.</p>
    </main>
  );
}
