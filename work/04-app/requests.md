# P4 requests between workers

Format: `[from] → [to]: what and why`. The orchestrator relays.

- [W2 design lead] → [app & QA engineer]: expose `readingMinutes` for each lesson from the content loader (word count of the MDX prose / 200, rounded, minimum 1). The lesson title-block strip shows it (DESIGN.md, Layout → Reading page).
- [W2 design lead] → [app & QA engineer]: please commit `main-app/.impeccable/` (Impeccable surface brief for the lesson reader; a development contract, not served by Next). Impeccable must run with cwd `main-app/` to find PRODUCT.md, DESIGN.md and this folder.
- [W2 design lead] → [app & QA engineer]: `main-app/app/fonts/` holds `OFL-*.txt` licence files next to the WOFF2s; exclude `app/fonts/**` from cspell and Biome if either scans it.
- [W2 design lead] → [visualization engineer]: the visual-language tokens, state grammar and player chrome look are in `main-app/DESIGN.md` → "Visual Language" (CSS custom properties `--color-viz-*`, `--viz-stroke*`, `--viz-dash`, `--dur-viz`, `--ease-draft`). Sections marked [W3] are yours to complete; if you need a new token, add it to the token block and note it here.

## Batch 2 (W2 → W1), 2026-09-23

- [W2] → [W1]: **Page-component props.** My proposal is `main-app/components/layout/props.ts` (LessonPageProps, ModulePageProps, CourseMapPageProps, HomePageProps, ProblemsPageProps, GlossaryPageProps, ProsePageProps for /start and /about; SearchPage takes none). If your `lib/content/types.ts` differs, tell me the shapes and I will map; otherwise please export these (or compatible) types and I will re-export from yours.
- [W2] → [W1]: **Code blocks: you pass raw strings, I highlight.** `components/content/CodeBlock.tsx` is an async server component: `<CodeBlock code={string} lang="python" filename?={string} highlight?={number[]} startLine?={number} caption?={string} variant?="normal"|"bad38" input?={string} output?={string} error?={{ type: string; traceback: string }} />`. It runs Shiki with `lib/content/shiki-theme.ts`. Your MDX map's `Code`/fenced `pre` read the files/lines, parse `highlight="3,4"`, `lines="1-5"` (→ `startLine`), and pass the committed `.in`/`.out`/traceback text. `Output` → `<OutputPanel output={string} />`.
- [W2] → [W1]: **Other MDX components (props I render):** `Callout {kind: "note"|"warning"|"grader-tip"; title?: string; children}`, `Term {id; definition: ReactNode; glossaryHref: string; children}` (you resolve the definition from glossary.yaml), `Details {summary; children}`, `ProblemLink {problem: ProblemView}`, `Practice {items: PracticeItemView[]}`, `JudgeLink {judge; kind: "home"|"signup"; href; children?}` (href from external-links.yaml), `DraftBadge`, `ComingSoon`.
- [W2] → [W1]: **UI copy.** Please export `getUiStrings()` (server-only, typed) from `lib/content/strings.ts`, backed by `content/ui/strings.yaml`. Keys I need are listed in `components/ui/ui-strings.ts` (interface `UiStrings` + the working default text, which you can copy into the YAML skeleton; P5 rewrites it). Until your loader exists my components read those defaults through `components/ui/ui-strings.ts`; I switch to your `getUiStrings()` as soon as it lands.
- [W2] → [W1]: **Read state / search hooks I call:** `useReadState(): { isRead(id): boolean; readAt(id): string | null; markRead(id): void; markUnread(id): void; ready: boolean; all(): Record<string,string> }` from `lib/read-state`; `loadSearch(): Promise<{ search(q: string): SearchResult[] }>` from `lib/search` with `SearchResult = { kind: "lesson"|"term"|"problem"; id; title; label; snippet?; href }`. Until they land I build against these signatures.
- [W3 visualization engineer] → [app & QA engineer]: **viz public API is published** (props stable; bodies are being filled in).
  - `components/viz/index.ts` exports `Figure`, `Diagram`, `StepThrough`, `CodeTrace`, `Scene` and `createVizComponents(moduleDir)`. In `lib/content/mdx-components.tsx` spread `...createVizComponents(absModuleDir)` into the map: it returns the five components with `frames`/`trace` paths bound to the lesson's module folder (they read `visuals/x.frames.json`, `examples/x.trace.json` at build time with `fs`). All five are async-safe server components.
  - Add `remarkFigureNumbers` from `lib/viz/remark-figure-numbers.ts` to the MDX `remarkPlugins` (it numbers `<Figure>`s 1, 2, 3 in document order; an author-typed `number` is an error).
  - `lib/viz/validate.ts` exports `validateVisualFile(path)` → `{ ok: true, kind, data } | { ok: false, kind, errors: string[] }` for `.frames.json`, `.trace.json`, `.viz.yaml`, `.trace.yaml` (never throws). It is pure TS; from a plain Node script load it with `node --import ./tools/viz/register.mjs your-script.ts` (the hook resolves extensionless relative imports and the `@/` alias under Node 24 type stripping; you may reuse it for `lib/content` scripts). For `prebuild`, you can also simply run `npm run check:viz -- --schema-only` once the script lines below exist.
  - Please add these `package.json` scripts (all point to files I own; each keeps the `node scripts/assert-contained.mjs &&` prefix):
    - `"gen:viz": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/gen-viz.ts"`
    - `"check:viz": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/check-viz.ts"`
    - `"viz:shots": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/viz-shots.ts"`
    and add `check:viz` to `verify:fast` as G-VIZ (`check:viz -- --schema-only` in `prebuild`, which needs no Python). `--scope <id>` is accepted by all three (module id such as `M4.13` or `fx.bfs`, a module folder name, or a visual file stem).
  - Ownership note: my Vitest files live in `tests/unit/viz/**` and G-VIZ fixtures in `tests/fixtures/gates/G-VIZ/**` (plan §7 "every gate has a fixture"); fixture visuals go into `tests/fixtures/content/**/visuals/**` and `**/examples/*.trace.{yaml,json}` next to your fixture modules.
  - Biome: please replace the two `content/**` ignores with `"!**/*.frames.json"` and `"!**/*.trace.json"` (generated files also live in `tests/fixtures/content/**` and `app/dev/viz/**`; `biome check --write` reformats them today, which breaks the G-VIZ no-diff rule).
- [W2] → [W1]: `biome.json`: please enable Tailwind directives for the CSS parser (`"css": { "parser": { "tailwindDirectives": true } }`); `app/globals.css` uses `@theme static` and `@utility`, which Biome currently reports as parse errors.
- [W2] → [W1]: `content/ui/strings.yaml` needs the keys I added to `UiStrings`: `home.lede` (string[]), `home.how` ({term,text}[]), `problems.notYet`, `notFound.cell`, `practice.judgeHome`, `practice.judgeSignup`. The working defaults are in `components/ui/ui-strings.ts`.
- [W2] → [W1]: MDX `Term` also needs `term` (the glossary heading shown in the popover). `JudgeLink` children are optional; the default text comes from strings.
- [W2] → [W1]: `LessonPage` renders the `practice` prop after the lesson body. Pass it only when the lesson's MDX does not already contain `<Practice />`; otherwise the list shows twice. My preference: omit `practice` from LessonPageProps' data wiring and let authors place `<Practice />`.
- [W2] → [W1]: `LessonPage` renders the `practice` prop after the lesson body. Pass it only when the lesson's MDX does not already contain `<Practice />`, or the list shows twice. My preference: don't pass it, and let authors place `<Practice />`.

## Batch 2, W1 replies + new items

- [W1] → orchestrator: correction — I mistakenly wrote placeholder components into `components/content/` (owned by W2) at 23:33. Confirmed W2 restored the real ones; I won't touch `components/content/`, `components/ui/`, `components/layout/`, `components/viz/`, `lib/viz/`, `tools/viz/`, `app/dev/viz/` again. Sorry for the noise.
- [W1] → [W2]: applied — `biome.json` now has `"css": { "parser": { "tailwindDirectives": true } } }`; `npm run lint` is clean on `app/globals.css`.
- [W1] → [W2]: `lib/content/mdx-components.tsx` now wires the fixed MDX component map (`Callout`, `Term`, `Details`, `Code`, `Output`, `ProblemLink`, `Practice`, `JudgeLink`, `DraftBadge`, `ComingSoon`, plus W3's viz five) against your barrel `components/content/index.ts` and the viz barrel. `Code` handles both `<Code file=… />` (reads `.py`/`.in`/`.out`/`.err` from the lesson's module dir) and fenced ```python / ```python bad38 blocks (via `lib/content/remark-fenced-code.ts`, which turns a fenced block into a `<Code lang variant>` element before your `CodeBlock` ever sees it). `Term` resolves `term`/`definition`/`glossaryHref` from `lib/content/glossary.ts`. `Practice` and `LessonPageProps.practice` — per your preference just above, my lesson page wiring will leave `practice` unset and rely on authors placing `<Practice />` in the MDX.
- [W1] → [W2]: `content/ui/strings.yaml` — I'll add `home.lede`, `home.how`, `problems.notYet`, `notFound.cell`, `practice.judgeHome`, `practice.judgeSignup` from your updated `components/ui/ui-strings.ts` defaults and extend `uiStringsSchema` to match, next.
- [W1] → orchestrator: added the shared `/dev/*` production-gate helper at `lib/content/dev-guard.ts` (`notFoundOutsideDevPreview()` — calls Next's `notFound()` unless `getBuildEnv() !== "production"`). W2 (`app/dev/design`) and W3 (`app/dev/viz`) should call it at the top of their route's page component. I'll add both routes to the E2E smoke list and the production-mode 404 check once those route files exist (still empty on disk as of this note).
- [W1] → [W3]: `npm run lint` reports one real error in your file, not a formatting issue: `lib/viz/schema.ts:618` — `lint/suspicious/useIterableCallbackReturn` on `file.presets.forEach((p, pi) => p.steps.forEach(...))`, because the outer `forEach` callback's arrow function implicitly returns the inner `forEach`'s return value (`undefined`, but the arrow body is an expression, so Biome sees a returned value). Fix: wrap the outer callback body in braces (`(p, pi) => { p.steps.forEach(...); }`) so nothing is returned. I did not edit your file.
- [W2] → [W1]: swapped my temporary adapters for `lib/read-state` and `lib/search/client` (deleted `components/ui/read-state.ts` and `search-source.ts`); `ui()` now returns `getUiStrings()` and the DEFAULTS are gone. `/dev/design` now calls `notFoundOutsideDevPreview()`.
- [W2] → [W1]: `lib/search/client.ts` bug: `loadSearch()` keeps a rejected promise in `cached`, so the search panel's "Try again" button can never recover after one failed fetch. Please clear it on failure, e.g. `cached = (async () => {…})().catch((e) => { cached = null; throw e; });`. Also, client components should import `@/lib/search/client`, not `@/lib/search` (the barrel re-exports the `fs`-based `build-index`).
- [W1] → [W2]: fixed — `loadSearch()` now clears `cached` on rejection before rethrowing, so a failed fetch can be retried. Confirmed none of my own files import the `@/lib/search` barrel (only `build-index` and `client` are used from their own paths).
- [W1] → [W3]: `npm run lint`/`typecheck` right now show parse errors in `lib/viz/geometry.ts:65-72` (`';' expected`, etc.) — looks like a file mid-write on your end (background agent still running as of this check), not something I'm touching. Flagging in case it's stale from an interrupted save; ignore if you're already past this point.
- [W1] → orchestrator/[W2]: fixed the `lib/search/client.ts` cached-rejection bug properly — reproduced it first with a new regression test (`tests/unit/search/client.test.ts`: swapped in the pre-fix source, confirmed the "retries after a failed fetch" test fails against it, restored the fix, confirmed green), then confirmed the fix. `loadSearch()` now clears `cached` before rethrowing on failure, so a second call (e.g. a "Try again" click) retries the network instead of replaying the same rejection.
- [W1] → orchestrator: applied the `course.yaml` leak-list corrections you relayed — Stage s0 retitled "Orientation" (was "Orientation and tooling") with a setup-free goal; M0.7 retitled "Online judges: WMOJ and DMOJ" (was "...the CCC Grader and DMOJ"). Also caught and fixed one I'd missed on the same sweep: M7.13 was titled "LCA and binary lifting on trees (bridge to CCO)", copied verbatim from the recon table despite my own file header saying CCO wording must not be copied — retitled to just "LCA and binary lifting on trees". Re-scanned every stage/module title and goal for score/medal/placement/setup wording; nothing else found.
- [W1] → [W2]: fixture course/lesson/module routes aren't written yet (still ahead of me: gates + `tests/fixtures/content/`). Will tell you here the moment there's a rendered fixture lesson to screenshot.
- [W1] → [W3]: applied — `biome.json`'s four `content/**`/`tests/fixtures/**` ignores replaced with `"!**/*.trace.json"` and `"!**/*.frames.json"`, so `app/dev/viz/**` is covered too. Confirmed valid JSON; full `biome check` still blocked on the in-flight `lib/viz/geometry.ts` parse errors from earlier, not this change.
- [W1] → orchestrator: `content:check` (G-SCHEMA, G-LINK-FMT, G-LINK-REF, G-LINK-PREF, G-LINK-LIVE, G-PREREQ) is written and wired (`npm run content:check`, TS via `tools/viz/register.mjs`, reused rather than reimplemented — thanks W3). First real run caught two genuine forward-reference bugs in `course.yaml` (M6.7→M6.11 and M7.1→M7.4, both verbatim from the recon table's prereq lists, which don't follow teaching order) — fixed by reordering within their stages; `content:check` is clean now. Registry verification also done: `tools/links/verify-judges.mjs` ran for real, 56/56 WMOJ problems `ok` (one title fix along the way: `ccc22s2` "Group Work" → "Good Groups", the judge's actual title), 63 DMOJ recorded `unverified`/`manual` pending the Manager's checklist (`work/04-app/dmoj-checklist.html`, `work/04-app/wmoj-verification-run-1.md`). Still ahead: `content:status`, `tools/pycheck` (G-PY-38/G-PY-RUN), `tools/style`, `tools/lint` (G-R14), G-SPELL/G-MDX wiring, gate fixtures, fixture course content.
- [W1] → orchestrator: `content:status` (ledger, `work/04-app/ledger.generated.md`, never hand-edited), `prebuild` (G-SCHEMA+G-LINK-FMT only, no Python, wired into `npm run build` via npm's own pre-script convention) and `tools/pycheck` (G-PY-38: ruff+vermin+`pypy38 -m py_compile` over every file-based example and every fenced ```python block, `bad38` must fail at least one; G-PY-RUN: byte-for-byte `.out`/`.err` reproduction under PyPy 3.8; `gen:outputs -- --scope`) are all written and wired into `verify:fast`. Every new gate has a `--root=`-isolated fixture under `tests/fixtures/gates/<gate-id>/` plus a Vitest test proving it fails there and the real tree stays clean (`tests/unit/gates/content-check.test.ts`, `check-python.test.ts` — 10 tests total, all green). `verify:full` (including `next build`) is green end to end right now.
- [W1] → [W3]: `npm run lint` currently fails on one file — `tools/viz/check.ts` has unformatted blocks (Biome would reformat ~6 spots, e.g. around lines 69-90, 117-134, 142-165, 181). Format-only, not a logic issue, and it's your file so I didn't touch it (`biome check . --write` from `main-app/` fixes it in one pass). Everything else is clean.
- [W1] → [W3]: `npm run lint` is clean now (your `tools/viz/check.ts` formatting landed, and `lib/viz/schema.ts:618` too — thanks). `npm run typecheck` has one real error, both in your `tests/unit/viz/remark-figure-numbers.test.ts`: line 20 and line 36, `TS2345`/`TS2322`, a test fixture object literal isn't assignable to `MdNode` because its `children: unknown[]` doesn't narrow to `MdNode[]`. Not my file, didn't touch it — likely just needs the fixture's `children` typed/cast to `MdNode[]` (or the nested node literals typed as `MdNode` before being placed in a `children` array).
- [W1] → orchestrator: all of batch 2's gates are now written and green: `content:check` (G-SCHEMA/G-LINK-FMT/G-LINK-REF/G-LINK-PREF/G-LINK-LIVE/G-PREREQ), `content:status`, `check:python` (G-PY-38/G-PY-RUN, incl. `gen:outputs`), `style:check` (G-STYLE, wired to `.claude/skills/avoid-ai-writing/detector/patterns.js` per D-040, `tools/style/thresholds.json` marked P4-default/P5-recalibrates), `lint:r14` (G-R14, `tools/lint/r14-banned.json` with the plan's exact patterns + a reviewed allow-list), `spell` (G-SPELL, cspell scoped to `content/**`+`tests/fixtures/content/**`+`*.md`, domain dictionary extended with real registry/design vocabulary — one real fix along the way, so it wasn't just a dictionary stuff-in: verifying a flagged word against the registry caught nothing wrong, all genuine terms), `lint:mdx` (G-MDX, remark-lint, already clean). Every gate has a `--root=`-isolated fixture under `tests/fixtures/gates/<gate-id>/` and a Vitest test proving fail-then-pass (now 12 gate-fixture tests total). All wired into `verify:fast`/`verify:full`. Next: fixture course content in `tests/fixtures/content/` (batch 2 item 6), which is the last big piece.
- [W1] → [W3]: thank you — I've started the fixture course (`tests/fixtures/content/`, stage `fx`, modules `M90.1`-`M90.3`) and see you're already filling in the `{/* W3: visual here */}` placeholders I left (M90.2's grid BFS `StepThrough`, M90.1's `CodeTrace` on `loops_and_lists.py` — both landed clean, typecheck's green). One more module still has open placeholders you might not have gotten to yet: `M90.3-fixture-visuals-and-formulas/lessons/visuals.mdx` wants a `Diagram` plus one fixture visual per remaining panel type (ArrayViz, GraphViz, TreeViz, TableViz, LineViz, PlotViz) — brief §5's "one module per remaining visualizer" ask, consolidated into one lesson for scope. No rush on all seven if that's too much for this pass; whatever you land I'll wire up / verify against the gates.
- [W1] → [W2]: the fixture course now renders — `/learn/fx/M90.1`, `/learn/fx/M90.2`, `/learn/fx/M90.3` and their lessons (e.g. `/learn/fx/M90.1/components-one`) all build. `next build` is green with real fixture pages in the static output. Worth a look/screenshot whenever you have a moment — flag anything that looks off and I'll fix the data side.
- [W1] → [W3]: all 8 panel visualizers plus the Scene landed in `M90.3` — `check:viz` reports 10 visuals, 0 problems, added `check:viz` to `verify:fast` as `G-VIZ` per your request. Caught and fixed two of my own gate scripts walking `visuals/*.viz.py` as if it were shown teaching code (a real `ruff` finding in `dijkstra.viz.py` started failing `check:python`, since your recorder scripts aren't held to the "shown on the grader" bar `.viz.py` is authoring tooling, checked by your own `check:viz`) — `check-python.ts`, `gen-outputs.ts` and `content-check.ts`'s G-PREREQ AST section now all skip `visuals/` directories. Thank you for moving fast on the fixture visuals, genuinely excellent collaboration.
- [W1] → orchestrator: batch 2 items 1-6 done; `verify:fast`/`verify:full` green end to end, including a full `next build` with the fixture course's 7 SSG pages live under `/learn/fx/*`. Item 7 (route wiring) confirmed via the build — every brief §4.10 route exists and builds, `/dev/design`+`/dev/viz` both call the shared production gate. Not done: "add `/dev/viz`+`/dev/design` to the E2E smoke list and the production-mode 404 check" — `tests/e2e/smoke.spec.ts` is a single stale test (still asserts batch-1 placeholder homepage copy against W2's real one) and a real production-mode 404 check needs its own build+server run, which reads as E2E-suite-scale (batch 3) work rather than a quick add-on; flagging instead of rushing it. Full detail in `work/04-app/_working/app-notes.md`.
- [W2] → [W1]: **bug in fenced code blocks inside MDX, found with the fixture course:** indentation is lost. In `/learn/fx/M90.1/components-one` the bad38 `match` block shows `case 3:` and `print(...)` at column 0, but the `.mdx` source is indented. File-based `<Code file>` blocks are fine. `remark-fenced-code.ts` passes the code as a JSX text child, and MDX JSX text children lose their leading whitespace before `childrenToText()` sees them. Suggested fix: pass the code as an attribute (`{ type: "mdxJsxAttribute", name: "code", value: node.value }`) and have `CodeTag` read `props.code`. A Python example with broken indentation is wrong code, so this matters beyond looks.
- [W2] → [W1]: **plural strings:** the course map shows "1 lessons". Please add `module.lessonCountOne: "{n} lesson"` and `home.modulesOne: "{n} module"` to `strings.yaml` and the schema. I'll switch `CourseMapPage` and `HomePage` to pick the `…One` form when n = 1 once they exist.
- [W2] → [W1]: markup changes that tests may touch (roles and text are unchanged, only DOM position):
  - IndexRow: the status tag (Coming soon, Draft) is now inside the meta span (grid column 3 from 640 px, a second line under the title on phones), not inline after the title.
  - Sidebar: a planned module's "Soon" tag is now its own grid cell after the title span.
  - Practice: "(same problem as …)" is now inline after the judge badge, not a block line.
  - LessonPage imports `katex/dist/katex.min.css`. It was missing, so math rendered twice (MathML plus the HTML fallback) and unstyled.
- [W1] → [W2]: both fixed, per CLAUDE.md's "reproduce first" rule.
  - Fenced-code indentation: reproduced first with a new failing unit test (`tests/unit/content/mdx-fenced-code.test.ts`, compiles the exact bad38 `match`/`case` fenced block from `M90.1/components-one.mdx` through the real `@mdx-js/mdx` `evaluate()` + `remarkFencedCode` pipeline, confirmed it fails pre-fix: indentation stripped, e.g. `case 3:` instead of `    case 3:`), then applied your suggested fix — `remark-fenced-code.ts` now emits `code` as an `mdxJsxAttribute` (never JSX children; JSX child text is whitespace-cleaned at compile time, an attribute string literal isn't), `CodeTag` in `mdx-components.tsx` now reads `props.code` first, falling back to `childrenToText(children)` for safety. Confirmed byte-for-byte through a real `next build`: grepped the built `.next/server/app/learn/fx/M90.1/components-one.html` RSC payload — the `code` string handed to `CopyButton` matches the `.mdx` source exactly (`"match len([1, 2, 3]):\n    case 3:\n        print(\"three items\")\n    case _:\n        print(\"some other count\")"`), and the rendered Shiki token stream for line 2 starts with a `"    "` token before `case`. Checked every other fixture fenced block (only one other exists, in the same file, no indentation to lose) — all clean. `test:unit`/`content:check`/`typecheck`/`build` all still green.
  - Plural strings: added `module.lessonCountOne: "{n} lesson"` and `home.modulesOne: "{n} module"` to `content/ui/strings.yaml` and as required fields on `uiStringsSchema` (`lib/content/schemas.ts`) — validation now fails if either is missing. Checked `strings.yaml` for any other `{n} <noun>` pattern that could pluralize wrong; `nav.stage` ("Stage {n}") and `lesson.lessonOf` ("{i} of {n}") aren't item counts, and `lesson.minutes` ("{n} min") doesn't visibly pluralize, so those two were the only ones. Over to you for `CourseMapPage`/`HomePage` wiring.
  - Noted for my own E2E selectors: IndexRow status tag / sidebar "Soon" cell / practice "(same problem as …)" moved in the DOM, roles and text unchanged — selecting by role/text, not position.
- [W2] → [W1], [W3]: width is now opt-in. Only `[data-wide]`, `figure`, `table` and display math take `--measure-wide` inside `.prose-sheet`; `data-exhibit` sets only the 2rem block spacing. Callouts, Details and Practice are at the prose measure (42rem). W3: `<figure class="vz-figure">` stays wide with no change needed. Rule table: DESIGN.md → Layout → Measure by block type.
- [W2] → [W1]: TitleBlockStrip markup changed: `dl` is now a grid (one row of cells), and the extra (Draft) cell is a sibling after the `dl`. Labels and values are unchanged.

## Batch 3, W3

- [W3] → [W2]: please add `--color-viz-changed` to the viz token block in `app/globals.css` (value: `var(--color-check-soft)`, i.e. #fef5c7). It is the "Just changed" fill in code traces. `components/viz/viz.css` reads `var(--color-viz-changed, var(--color-check-soft))` until then, and `tests/unit/viz/tokens.test.ts` lists it as pending. DESIGN.md → Visual Language → "Tokens added [W3]" records it.
- [W3] → [W1]: fixture visuals are in, filling your placeholders (I edited only the `{/* W3: visual here … */}` comments, plus one lead-in sentence in `M90.3/lessons/visuals.mdx` that cited the brief):
  - M90.1: `examples/loops_and_lists.trace.{yaml,json}` plus a `<CodeTrace>` in `components-two.mdx`.
  - M90.2: `visuals/grid-bfs.*` plus `examples/bfs_grid.py`, a GridViz + queue with 3 presets in `technique.mdx`.
  - M90.3: `visuals/{pair-sum,dijkstra,fib-calls,grid-paths,merge-intervals,first-square}.*` plus `examples/*.py`, one per remaining visualizer. Dijkstra is GraphViz + heap. There is also a `<Diagram>` of the finished table.
  - Every frames visual declares a consistency check against its example, and `check:viz` runs them.
  - `check:python -- --scope M90` is clean (ruff, vermin and py_compile over the `.viz.py` files too).
- [W3] → [W1]: `scripts/port-lease.mjs` `checkPortFree` binds `127.0.0.1` only, but `next dev` listens on `::`. On macOS both binds succeed, so a port with a running dev server that has no lease is reported free. Seen today: someone's unleased `next dev -p 4100` was up, and `leasePort` still handed out 4100 (EADDRINUSE). Please probe `server.listen(port)` with no host (dual-stack) as well as `127.0.0.1`. `tools/viz/viz-shots.ts` works around it for now by connecting to each leased port and skipping ports that answer.
- [W3] → orchestrator, [W1]: a `next build` that writes to `.next` kills any running `next dev`, because they share the dist dir. It happened twice this session: my dev server died silently at 00:39, right when a build wrote `.next/BUILD_ID`. Next 16 also allows only one `next dev` per dist dir, and `viz:shots` now reuses an existing one when it finds it. Proposals:
  - Give `npm run build` (and `verify:full`'s build) their own `NEXT_DIST_DIR`, like `check:production-mode` already does with `.next-prod-check`, so builds and dev servers never share a directory.
  - Note that `next build` with any non-default `NEXT_DIST_DIR` rewrites `tsconfig.json` (adds `<dir>/types` includes and reflows the arrays) and `next-env.d.ts`. I built into `test-results/w3-build` to measure the player chunk, then restored both files byte for byte. A dedicated dir would need its include lines added once and committed.
- [W3] → [W1]: gate wiring, all three scripts now exist:
  - `check:viz` (G-VIZ) accepts `--scope <id>`, `--schema-only` and `--root=<dir>`. Every rule has a failing fixture under `tests/fixtures/gates/G-VIZ/<rule>/` (schema, missing, stale, caption, alt, consistency, size, steps, text-size, library, orphan, tree-ids), and `tests/unit/viz/gate-viz.test.ts` proves each one fails and the real tree passes.
  - `viz:shots` needs a browser and takes about 1 min, so it belongs in `verify:full`, not `verify:fast`. By default it leases a port, starts or reuses `next dev`, shoots every visual on `/dev/viz` and on every lesson reachable from `/learn` at 390/768/1440 (first/middle/last step), and fails on text below 14/12 px, horizontal page scroll, or console errors and warnings. Output goes to `test-results/viz-shots/`.
- [W3] → [W1]: FYI, `npm run lint` fails on `tests/e2e/support/fixtures.ts:26` (an unused suppression plus formatting). That file is yours; I didn't touch it.
- [W3] → all: `lib/viz/states.ts` now holds the state vocabulary (`STATE_CODES`, `stateOf`, `VizState`) without Zod. `lib/viz/schema.ts` re-exports it. Client code must import runtime values from `states`, never from `schema`, or Zod ends up in the browser chunk. That is exactly how the lazy player chunk had grown to 110.6 kB gzip; it is 18.3 kB now.
- [W3] → [W1]: FYI, `npm run typecheck` fails on `tests/e2e/copy-button.spec.ts(47,10)` (TS2532, object possibly undefined). That file is yours too.
- [W2] → [W3]: `--color-viz-changed: var(--color-check-soft)` is now in `app/globals.css` and in the DESIGN.md token block and token list; the value is confirmed as check-soft. Your `viz.css` fallback can be dropped when convenient. The code trace's "Just changed" legend swatch and the stage items both render through `[data-s="changed"]`, so they now share the defined token. Please update your [W3] note in DESIGN.md (Visual Language), which still says "until it lands".
- [W3] → [W1]: player DOM contract for your E2E tests. There is no player spec in `tests/e2e/` yet; tell me here when one lands and I'll run it with your harness.
  - Group: `[role="group"][data-player]`; `data-ready` appears once it is hydrated.
  - Controls: `[data-ctl="restart|prev|play|next|scrub|preset-<i>|speed-<id>"]`.
  - Caption: `.vz-caption`, with code in `code.vz-cap-code`.
  - Frozen gallery demos: `.vz-mount[data-frozen]`.
  - Keys: ←/→, Space, Home/End, only while focus is inside the group.
- [W1] → orchestrator: design-review.md A1-1 through A1-5 and A1-8, all done.
  - A1-1 (broken WMOJ sign-up link): `external-links.yaml`'s `wmoj-signup` now points at `https://wmoj.ca/auth/signup` (the live-confirmed path). `dmoj-signup` changed to `https://dmoj.ca/accounts/register/` — Cloudflare blocks live verification of DMOJ from here, so this is a best-guess pending the Manager's DMOJ checklist, flagged with a comment in the YAML.
  - A1-2 (single source for judge home/sign-up URLs): removed `JUDGE_HOME`/`JUDGE_SIGNUP`/`judgeLinkUrl()` from `lib/registry/judge-url.ts` entirely (it now only builds problem URLs). `JudgeLinkTag` in `mdx-components.tsx` reads `getExternalLink(`${judge}-${kind}`)` from `external-links.yaml` instead.
  - A1-3 (C21, no setup content): removed `pypy-downloads`/`python-downloads` from `external-links.yaml`. Added two G-R14 patterns (`python-download-link`, `pypy-download-link`) so re-adding either link to lesson prose fails the build even without the registry entry; fixture + test added.
  - A1-4 (MDX map): `DraftBadge`/`ComingSoon` removed from the component map (authors can no longer place a Draft badge; it's derived from module status). `OutputTag` and `<Code file>` both now go through a new `resolveModulePath(moduleDir, file)` guard (`lib/content/code-block-data.ts`) that rejects any path resolving outside the module dir — 4 unit tests (ordinary path, `../` traversal, absolute path, module dir itself). Static gate fixture: content:check's G-SCHEMA now statically checks every lesson MDX file's JSX tag names against the fixed component map (new `lib/content/mdx-component-names.ts`, a plain-data module so the Node type-stripping gate script can read it without a JSX transform; kept in sync with the real map by `tests/unit/content/mdx-component-names.test.ts`) — this catches an unknown/invented component at `content:check` time, before it would otherwise only surface when `next build` actually renders the page. Fixture at `tests/fixtures/gates/G-SCHEMA/stages/s0-test/M1.1-a/` (a lesson using `<WalkthroughBox>`), test in `tests/unit/gates/content-check.test.ts`.
  - A1-5 (build-env visibility): added `scripts/gates/print-build-env.ts`, run first by `prebuild.mjs`, printing the resolved `getBuildEnv()` result plus the raw `VERCEL_ENV`/`ETCCC_ENV` values and what it means for draft visibility — so the very first line of a Vercel build log proves which branch resolved. 4 regression tests (`tests/unit/gates/print-build-env.test.ts`) cover all of `getBuildEnv()`'s branches.
  - A1-8 (read-state shape-checking): `lib/read-state`'s `read()` now goes through a new pure `parseReadMap()` (rejects non-object/array/null, drops non-string or unparseable-date values) and `isRead`/`readAt` use `Object.hasOwn` instead of `in`/bracket access. 8 unit tests in `tests/unit/read-state.test.ts` cover corrupt storage, arrays, bare scalars, non-string values, unparseable dates, and own-vs-inherited keys.
  - Also fixed along the way (not A-numbered, found while re-running the full suite): `vitest.config.ts` had no alias mirroring tsconfig's `"@/*": ["./*"]`, so any unit test that transitively imported one of the 52 files under `lib/`/`components/` using a `@/` import (e.g. `mdx-components.tsx` → `@/components/ui/Badge`) would fail to resolve under Vitest even though the same import is fine under Next/tsc — a latent gap that happened to never get hit until my new `mdx-component-names.test.ts` imported `mdx-components.tsx` directly. Added a `resolve.alias` entry; no other test was affected (all were already avoiding that transitive path).
  - Full `verify:fast`-equivalent pass after all of the above together: `lint` (209 files, clean), `typecheck` (clean), `test:unit` (27 files, 156 tests, all green), `content:check` (0 errors, 1 pre-existing warning: `ccc19j1` still `unverified` in `verified.json`, unrelated to any of this).
  - Not touching, flagging instead: `npm run spell` currently reports 4 unknown words in `DESIGN.md` ("blockquotes", "logn", "nlogn", "recolour") — not in my scope and DESIGN.md has uncommitted changes from someone else right now, so I didn't add them to the domain dictionary or touch the file. Easy fix whenever convenient: add the words to `dictionaries/et-ccc-domain.txt` if they're intentional (they read like legitimate terms, "recolour" being the likely-intentional British/Canadian spelling), or fix the typo in DESIGN.md if not.

## W2 → W3: visual-language critique

Impeccable `critique`, run as a single-context review because workers may not start subagents. Assessment A was done first: W3's `review3` shots at 390/768/1440, including `all-steps/`, compared against DESIGN.md → Visual Language. Assessment B ran afterwards: `impeccable detect --json components/viz app/dev/viz` gave 1 advisory, handled in item 10. The heap panel was skipped as asked. Shot paths are relative to `work/04-app/_scratch/shots/w3/review3/`.

Overall the visuals read as the same product as the reading pages: same inks, lettering, 6 px frames, sheet-sunk control strip and segmented controls. The state grammar survives greyscale. The findings are mostly about a single rule being broken or a data mismatch, not a second style.

1. **blocking**: Code trace clips source code. Line 11 shows `return [n] + countdown(n - 1` at 1440 and `…countdown(n` at 390; the closing parenthesis (and more) is cut off. Code shown to a learner must be complete. Let the code pane scroll horizontally like the Code block (`overflow-x: auto`, focusable), or size the pane to its longest line before it takes its 58% share. `dev_viz-1440-trace-recursion-first.png`, `dev_viz-390-trace-recursion-first.png`
2. **should-fix**: stdin scene has two current edges at once, and the caption disagrees with the picture. At step 2/4 both the program line `words = input().split()` and the variable `n → 5` carry the 3 px current edge on check fill (One Active Edge Rule). The caption says "Line 1 runs", but the current program line is line 2. Either the new value is "Just changed" (check-soft, 1.5 px) and line 1 is current, or the step is split so cause and effect come one at a time. `dev_viz-390-scene-stdin-mid.png`
3. **should-fix**: stdin scene variable value is drawn as a full-width bar. `n → 5` renders as a ~230-unit-wide cell, which reads as a progress bar, not a value. Every other value in the library is a cell sized to its content (value width + 14, at least 40). `dev_viz-390-scene-stdin-mid.png`
4. **should-fix**: The array-compare sample contradicts itself. The bracket reads `5 < 8` (i at index 1, j at index 2), but the plum "Compared" cells are 8 and 11 (indices 2 and 3). Highlight the two cells the comparison names. `dev_viz-1440-array-compare-first.png`
5. **should-fix**: Plot gridlines use construction blue. The horizontal lines at y = 10, 20, 30 are the guide colour, but the Principles reserve construction blue for the decorative background grid and never for data. Draw chart gridlines in `rule` at 1 px, or drop them and let the tick labels and background grid carry the reading. `dev_viz-1440-plot-first.png`
6. **should-fix**: Frozen gallery pictures lose control selection. "Player, middle" shows neither preset nor any speed selected; the same player live, and the trace picture, show "Open grid" and "1×" in ink. Frozen pictures are the visual baselines, so they must show the real control state. `dev_viz-1440-player-mid-first.png` (compare `dev_viz-390-player-initial-mid.png`)
7. **should-fix**: The judging scene shows "AC" as the verdict with no words. DESIGN.md (Scenes) says "the verdict per test in words". A first-time learner does not know "AC". Write "Accepted", "Wrong answer" and so on (strings through the scene props or UI strings), with the short code at most as a secondary label. `dev_viz-1440-scene-judging-mid.png`
8. **nit**: Code trace at 390 leaves a ~100 px empty band between the frames/objects panel and "Output so far" (the union box of all steps). Consider placing Output directly under the code pane on phones, or reserving the union height below the output instead of above it. `dev_viz-390-trace-recursion-first.png`
9. **nit**: The tree's current node (f(4)) has the 3 px edge but no caret above it, while grids, arrays and graph nodes do. The State vocabulary asks for "heaviest outline plus a pointer label or caret above it". `dev_viz-1440-tree-first.png`
10. **nit (detector, advisory)**: `viz.css:194/199` literal `font-size: 16px/14px`. These are SVG natural units (they scale with the stage), not CSS text sizes, so this is a false positive for the page ramp. I've now documented the natural sizes in DESIGN.md → Typography in visuals, so no change is needed. If you want the detector quiet, use `ignore-value 14px` for this file only.
11. **nit**: The plot's "mid" line is a 3 px dashed ink line. Heavy means current and dashed means provisional/queued, so the cues mix. A sweep line that *is* the current position should be solid 3 px, or stay 1.5 px dashed with only the marker carrying the current edge. `dev_viz-1440-plot-first.png`

Fixed on my side (W2): DESIGN.md [W2] now says (a) how "Compared" is shown on graph edges and tree links (plum 2 px plus the comparison in the caption; the bracket is for cells and array items), (b) chart gridlines are `rule`, never construction blue, and (c) the natural SVG lettering sizes (values 16, labels and titles 14 units).
- [W2] → [W1]: spell gate, DESIGN.md: I reworded "blockquotes" to "block quotes" (my section). The other three are in W3's [W3] sections and are genuine: `logn` and `nlogn` are the `growth-rates` scene's prop values (inside backticks), and "recolour" is the Canadian spelling the course uses. Please add all three to `dictionaries/et-ccc-domain.txt`, or have cspell ignore inline code spans in `*.md`, which would cover the prop names for good.
- [W2] → [W1]: G-PAGE is now in `tests/a11y/` (`pages.spec.ts`, plus `routes.ts` and `settle.ts`, which `tests/visual/pages/` also uses). The orchestrator asked for Chromium **and WebKit**, but `playwright.a11y.config.ts` uses `chromiumOnlyProjects()`. The spec runs unchanged on WebKit if the config switches to `fullDeviceProjects()`; your call as config owner, and I didn't edit it. Current state: Chromium at 3 viewports is 82 passed (35 skipped by design: overflow checks off-390 and the drawer at 1440), and `--repeat-each 10 --retries 0` gives 820/820.

### W3 replies to the visual-language critique (2026-09-24)

Shots after the fixes: `work/04-app/_scratch/shots/w3/review5/` (`sheet-critique.png` collects the items).

1. **Fixed.** The trace is now a wrapping row: the code column (code + output) keeps its natural width and the frames/objects panel never drops below its natural width × 0.875. When they don't fit side by side, the row wraps into code, output, frames. At 1440, line 11 now shows whole. DESIGN.md's code block rule applies on phones: a long line scrolls inside the focusable pane, and nothing wraps. New `viz:shots` check "code clipped": it fails on any cut-off line side by side or in a stacked pane wider than a phone, and on a phone it fails if the pane isn't keyboard-scrollable. I proved it by restoring the old `fit-content(58%)` column, which gives `line 11 is cut off by 25 px`.
2. **Fixed.** The new value is now "just changed" (check-soft). The program line about to run keeps the one current edge. Captions read "Line 1 ran: … Line 2 runs next." / "The program ends here."
3. **Fixed.** Each variable cell is sized to its own value (value width + 14, at least 40).
4. **Fixed.** The sample's compared cells are now indices 1 and 2 (`5 < 8`).
5. **Fixed.** Chart gridlines are `rule` at 1 px.
6. **Fixed (a real bug).** Every instance of a visual shared one radio `name`, so checking one picture's preset/speed unchecked the other's. The groups are now named per instance (`useId`).
7. **Fixed.** The verdict is on its own full-width row in words, e.g. "Verdict: Time limit exceeded (TLE)". The code stays under each test case only as a secondary label. A rejected verdict stays plain ink, because the invalid strike would cross the words.
8. **Fixed** by item 1's order: code, output, then frames. The union height now sits last.
9. **Fixed.** A current tree node has its caret. The heap's tree is the exception: its caret stays on the array cell, so it is drawn once.
10. **No change**, as you suggested; the values are natural SVG units.
11. **Fixed.** Sweeps (PlotViz `vline`, LineViz sweep) are 1.5 px dashed ink and never heavy. The markers or points carry the current edge.
DESIGN.md [W3] now records items 1, 2, 3, 5, 7, 9 and 11.
- [W1] → orchestrator: items 1-5 from your reassigned list are done (item 6 — search retry, blocked storage, no-request-leaves-origin, route-list completeness — was already done from before; confirmed still passing).
  1. `scripts/deployed-url.mjs`: resolves the deployed preview URL for the current HEAD sha via `.tooling/bin/git`/`.tooling/bin/gh api repos/.../deployments`, polling statuses until `success` with a timeout (`--timeout=`). Pure polling logic is unit-tested against recorded-shape JSON fixtures (`tests/fixtures/deployed-url/`, 14 tests, fake clock, no real network/waiting). Confirmed working against the real repo (correctly times out with a clear message — no deployment exists yet since nothing's on Vercel).
  2. `verify.mjs` rewritten to support `fast`/`full`/`preview`, each gate now a small `{id, describe, run()}` object instead of a bare npm-script name (needed since Playwright's extra args, e.g. `--fail-on-flaky-tests`, can't reliably pass through `npm run <script> --`). `full` = fast gates + G-BUILD (new `scripts/gates/verify-build.mjs`, its own `.next-verify-build` dist dir, never `npm run build`'s plain `.next`) + the production-mode proof + G-E2E/G-VISUAL/G-PAGE (each with `--fail-on-flaky-tests`) + G-LINKS-INT + viz:shots. `preview` resolves `PLAYWRIGHT_BASE_URL` via `deployed-url.mjs` then runs G-E2E/G-VISUAL/G-PAGE against it (no local server/port lease — already built into `run-playwright.mjs`/`playwright-shared.ts`). G-PERF and G-LINK-EXT are deliberately NOT wired into any verify mode (plan §7 times them "P4 exit, release" / "release", same as `links:verify`). `verify:fast` and `verify:build` both confirmed green end to end for real.
  3. G-LINKS-INT: `scripts/links-internal.mjs`, linkinator over a real local production build, `checkFragments: true`, external domains skipped (`linksToSkip`) since that's G-LINK-EXT's job and this gate has to stay network-deterministic. Reproduced the bug first (a temporary broken link in a fixture lesson, reverted after) — caught it correctly (2 broken entries, right parent), then ran clean on the real tree (27 checked, 0 broken).
  4. G-PERF: `scripts/perf-budgets.mjs`, budgets in `work/04-app/perf-budgets.md`. Checks home/course-map/the fixture lesson with the most visuals (M90.3)/`/problems`: first-load JS from Turbopack's own `diagnostics/route-bundle-stats.json` (Next 16 no longer prints a size table), plus a Lighthouse performance-only audit (score/LCP/TBT/CLS). Worth flagging: the first attempt (Lighthouse's own `chrome-launcher`) leaked a Crashpad `settings.dat` file to `~/Library/Application Support/.../Crashpad/` outside the workspace, confirmed via `contain-snapshot` — unfixable via `--user-data-dir`/`--disable-crash-reporter`/`--disable-breakpad`/`--crash-dumps-dir`/an overridden `$HOME` (all tried, all still leaked). Fixed by launching Chromium through Playwright itself instead (`chromium.launch()` + `--remote-debugging-port`, Lighthouse just attaches to the CDP port) — verified with a clean `contain-snapshot diff` (0 UNLISTED). Removed the now-unneeded `chrome-launcher` devDependency. Full write-up in perf-budgets.md, including the measured baseline (perf score 96-99, TBT 3-9ms, CLS 0.000 across three runs; LCP noisy, 2100-3065ms, read as shared-machine contention not a real signal) and why each budget number is what it is.
  5. G-LINK-EXT: `tools/links/verify-external.mjs` (`npm run links:external`, manual only), same body-check technique as `verify-judges.mjs` for wmoj.ca entries (the `NEXT_HTTP_ERROR_FALLBACK` marker, not just status 200), dmoj.ca entries recorded `manual` (Cloudflare, no circumvention). Ran for real: 6/6 non-DMOJ links ok, including a live re-confirmation that `wmoj-signup`'s new URL (A1-1) actually resolves. `work/04-app/external-links-run-1.md` written.
  Also: added `.next-links-check`/`.next-verify-build`/`.next-perf` to `tsconfig.json`'s `exclude`/`include` and `cspell.json`'s `ignorePaths` (same pattern as `.next-local`/`.next-prod-check`); noticed and included W3's `.next-w3viz` in the same two files while I was there (mechanical, not a design decision). Full `lint`/`typecheck`/`test:unit` (229 files clean, 199/199 tests) and `content:check` (clean) all green after every change above, together with what W2/W3 landed concurrently.
  Not done, and now the real remaining scope: W3's own player/viz E2E+visual specs and W2's a11y/page-visual specs (their job now, per your reassignment — I see `tests/a11y/`, `tests/e2e/viz/`, `tests/visual/` already appearing, good) need a real `verify:full` run once they're substantially filled in to prove the whole pipeline end to end — I haven't run that (would need their content to be meaningful, and it's a long run). `verify:preview` is wired and tested for its "nothing deployed yet" failure path but can't be tested against a real success path until Vercel is linked.
