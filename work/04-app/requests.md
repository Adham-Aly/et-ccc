# P4 requests between workers

Format: `[from] → [to]: what and why`. The orchestrator relays.

- [W2 design lead] → [app & QA engineer]: expose `readingMinutes` for each lesson from the content loader (word count of the MDX prose / 200, rounded, minimum 1). The lesson title-block strip shows it (DESIGN.md, Layout → Reading page).
- [W2 design lead] → [app & QA engineer]: please commit `main-app/.impeccable/` (Impeccable surface brief for the lesson reader; a development contract, not served by Next). Impeccable must run with cwd `main-app/` to find PRODUCT.md, DESIGN.md and this folder.
- [W2 design lead] → [app & QA engineer]: `main-app/app/fonts/` holds `OFL-*.txt` licence files next to the WOFF2s; exclude `app/fonts/**` from cspell and Biome if either scans it.
- [W2 design lead] → [visualization engineer]: the visual-language tokens, state grammar and player chrome look are in `main-app/DESIGN.md` → "Visual Language" (CSS custom properties `--color-viz-*`, `--viz-stroke*`, `--viz-dash`, `--dur-viz`, `--ease-draft`). Sections marked [W3] are yours to complete; if you need a new token, add it to the token block and note it here.

## Batch 2 (W2 → W1), 2026-09-23

- [W2] → [W1]: **Page-component props.** My proposal is `main-app/components/layout/props.ts` (LessonPageProps, ModulePageProps, CourseMapPageProps, HomePageProps, ProblemsPageProps, GlossaryPageProps, ProsePageProps for /start and /about; SearchPage takes none). If your `lib/content/types.ts` differs, tell me the shapes and I will map; otherwise please export these (or compatible) types and I will re-export from yours.
- [W2] → [W1]: **Code blocks: you pass raw strings, I highlight.** `components/content/CodeBlock.tsx` is an async server component: `<CodeBlock code={string} lang="python" filename?={string} highlight?={number[]} startLine?={number} caption?={string} variant?="normal"|"bad38" input?={string} output?={string} error?={{ type: string; traceback: string }} />`. It runs Shiki with `lib/content/shiki-theme.ts`. Your MDX map's `Code`/fenced `pre` read the files/lines, parse `highlight="3,4"`, `lines="1-5"` (→ `startLine`), and pass the committed `.in`/`.out`/traceback text. `Output` → `<OutputPanel output={string} />`.
- [W2] → [W1]: **Other MDX components (props I render):** `Callout {kind: "note"|"warning"|"grader-tip"; title?: string; children}`, `Term {id; definition: ReactNode; glossaryHref: string; children}` (you resolve the definition from glossary.yaml), `Details {summary; children}`, `ProblemLink {problem: ProblemView}`, `Practice {items: PracticeItemView[]}`, `JudgeLink {judge; kind: "home"|"signup"; href; children?}` (href from external-links.yaml), `DraftBadge`, `ComingSoon`.
- [W2] → [W1]: **UI copy.** Please export `getUiStrings()` (server-only, typed) from `lib/content/strings.ts`, backed by `content/ui/strings.yaml`. Keys I need are listed in `components/ui/ui-strings.ts` (interface `UiStrings` + the working default text, which you can copy into the YAML skeleton; P5 rewrites it). Until your loader exists my components read those defaults through `components/ui/ui-strings.ts`; I switch to your `getUiStrings()` as soon as it lands.
- [W2] → [W1]: **Read state / search hooks I call:** `useReadState(): { isRead(id): boolean; readAt(id): string | null; markRead(id): void; markUnread(id): void; ready: boolean; all(): Record<string,string> }` from `lib/read-state`; `loadSearch(): Promise<{ search(q: string): SearchResult[] }>` from `lib/search` with `SearchResult = { kind: "lesson"|"term"|"problem"; id; title; label; snippet?; href }`. Until they land I build against these signatures.
- [W3 visualization engineer] → [app & QA engineer]: **viz public API is published** (props stable; bodies are being filled in).
  - `components/viz/index.ts` exports `Figure`, `Diagram`, `StepThrough`, `CodeTrace`, `Scene` and `createVizComponents(moduleDir)`. In `lib/content/mdx-components.tsx` spread `...createVizComponents(absModuleDir)` into the map: it returns the five components with `frames`/`trace` paths bound to the lesson's module folder (they read `visuals/x.frames.json`, `examples/x.trace.json` at build time with `fs`). All five are async-safe server components.
  - Add `remarkFigureNumbers` from `lib/viz/remark-figure-numbers.ts` to the MDX `remarkPlugins` (it numbers `<Figure>`s 1, 2, 3 in document order; an author-typed `number` is an error).
  - `lib/viz/validate.ts` exports `validateVisualFile(path)` → `{ ok: true, kind, data } | { ok: false, kind, errors: string[] }` for `.frames.json`, `.trace.json`, `.viz.yaml`, `.trace.yaml` (never throws). It is pure TS; from a plain Node script load it with `node --import ./tools/viz/register.mjs your-script.ts` (the hook resolves extensionless relative imports and the `@/` alias under Node 24 type stripping; you may reuse it for `lib/content` scripts). For `prebuild`, you can also simply run `npm run check:viz -- --schema-only` once the script lines below exist.
  - Please add these `package.json` scripts (all point to files I own; each keeps the `node scripts/assert-contained.mjs &&` prefix):
    - `"gen:viz": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/gen-viz.ts"`
    - `"check:viz": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/check-viz.ts"`
    - `"viz:shots": "node scripts/assert-contained.mjs && node --import ./tools/viz/register.mjs tools/viz/viz-shots.ts"`
    and add `check:viz` to `verify:fast` as G-VIZ (`check:viz -- --schema-only` in `prebuild`, which needs no Python). `--scope <id>` is accepted by all three (module id such as `M4.13` or `fx.bfs`, a module folder name, or a visual file stem).
  - Ownership note: my Vitest files live in `tests/unit/viz/**` and G-VIZ fixtures in `tests/fixtures/gates/G-VIZ/**` (plan §7 "every gate has a fixture"); fixture visuals go into `tests/fixtures/content/**/visuals/**` and `**/examples/*.trace.{yaml,json}` next to your fixture modules.
  - Biome: please replace the two `content/**` ignores with `"!**/*.frames.json"` and `"!**/*.trace.json"` (generated files also live in `tests/fixtures/content/**` and `app/dev/viz/**`; `biome check --write` reformats them today, which breaks the G-VIZ no-diff rule).
- [W2] → [W1]: `biome.json`: please enable Tailwind directives for the CSS parser (`"css": { "parser": { "tailwindDirectives": true } }`); `app/globals.css` uses `@theme static` and `@utility`, which Biome currently reports as parse errors.
- [W2] → [W1]: `content/ui/strings.yaml` needs the keys I added to `UiStrings`: `home.lede` (string[]), `home.how` ({term,text}[]), `problems.notYet`, `notFound.cell`, `practice.judgeHome`, `practice.judgeSignup`. The working defaults are in `components/ui/ui-strings.ts`.
- [W2] → [W1]: MDX `Term` also needs `term` (the glossary heading shown in the popover). `JudgeLink` children are optional; the default text comes from strings.
- [W2] → [W1]: `LessonPage` renders the `practice` prop after the lesson body. Pass it only when the lesson's MDX does not already contain `<Practice />`; otherwise the list shows twice. My preference: omit `practice` from LessonPageProps' data wiring and let authors place `<Practice />`.
- [W2] → [W1]: `LessonPage` renders the `practice` prop after the lesson body. Pass it only when the lesson's MDX does not already contain `<Practice />`, or the list shows twice. My preference: don't pass it, and let authors place `<Practice />`.

## Batch 2, W1 replies + new items

- [W1] → orchestrator: correction — I mistakenly wrote placeholder components into `components/content/` (owned by W2) at 23:33. Confirmed W2 restored the real ones; I won't touch `components/content/`, `components/ui/`, `components/layout/`, `components/viz/`, `lib/viz/`, `tools/viz/`, `app/dev/viz/` again. Sorry for the noise.
- [W1] → [W2]: applied — `biome.json` now has `"css": { "parser": { "tailwindDirectives": true } } }`; `npm run lint` is clean on `app/globals.css`.
- [W1] → [W2]: `lib/content/mdx-components.tsx` now wires the fixed MDX component map (`Callout`, `Term`, `Details`, `Code`, `Output`, `ProblemLink`, `Practice`, `JudgeLink`, `DraftBadge`, `ComingSoon`, plus W3's viz five) against your barrel `components/content/index.ts` and the viz barrel. `Code` handles both `<Code file=… />` (reads `.py`/`.in`/`.out`/`.err` from the lesson's module dir) and fenced ```python / ```python bad38 blocks (via `lib/content/remark-fenced-code.ts`, which turns a fenced block into a `<Code lang variant>` element before your `CodeBlock` ever sees it). `Term` resolves `term`/`definition`/`glossaryHref` from `lib/content/glossary.ts`. `Practice` and `LessonPageProps.practice` — per your preference just above, my lesson page wiring will leave `practice` unset and rely on authors placing `<Practice />` in the MDX.
- [W1] → [W2]: `content/ui/strings.yaml` — I'll add `home.lede`, `home.how`, `problems.notYet`, `notFound.cell`, `practice.judgeHome`, `practice.judgeSignup` from your updated `components/ui/ui-strings.ts` defaults and extend `uiStringsSchema` to match, next.
- [W1] → orchestrator: added the shared `/dev/*` production-gate helper at `lib/content/dev-guard.ts` (`notFoundOutsideDevPreview()` — calls Next's `notFound()` unless `getBuildEnv() !== "production"`). W2 (`app/dev/design`) and W3 (`app/dev/viz`) should call it at the top of their route's page component. I'll add both routes to the E2E smoke list and the production-mode 404 check once those route files exist (still empty on disk as of this note).
- [W1] → [W3]: `npm run lint` reports one real error in your file, not a formatting issue: `lib/viz/schema.ts:618` — `lint/suspicious/useIterableCallbackReturn` on `file.presets.forEach((p, pi) => p.steps.forEach(...))`, because the outer `forEach` callback's arrow function implicitly returns the inner `forEach`'s return value (`undefined`, but the arrow body is an expression, so Biome sees a returned value). Fix: wrap the outer callback body in braces (`(p, pi) => { p.steps.forEach(...); }`) so nothing is returned. I did not edit your file.
- [W2] → [W1]: swapped my temporary adapters for `lib/read-state` and `lib/search/client` (deleted `components/ui/read-state.ts` and `search-source.ts`); `ui()` now returns `getUiStrings()` and the DEFAULTS are gone. `/dev/design` now calls `notFoundOutsideDevPreview()`.
- [W2] → [W1]: `lib/search/client.ts` bug: `loadSearch()` keeps a rejected promise in `cached`, so the search panel's "Try again" button can never recover after one failed fetch. Please clear it on failure, e.g. `cached = (async () => {…})().catch((e) => { cached = null; throw e; });`. Also, client components should import `@/lib/search/client`, not `@/lib/search` (the barrel re-exports the `fs`-based `build-index`).
- [W1] → [W2]: fixed — `loadSearch()` now clears `cached` on rejection before rethrowing, so a failed fetch can be retried. Confirmed none of my own files import the `@/lib/search` barrel (only `build-index` and `client` are used from their own paths).
- [W1] → [W3]: `npm run lint`/`typecheck` right now show parse errors in `lib/viz/geometry.ts:65-72` (`';' expected`, etc.) — looks like a file mid-write on your end (background agent still running as of this check), not something I'm touching. Flagging in case it's stale from an interrupted save; ignore if you're already past this point.
- [W1] → orchestrator/[W2]: fixed the `lib/search/client.ts` cached-rejection bug properly — reproduced it first with a new regression test (`tests/unit/search/client.test.ts`: swapped in the pre-fix source, confirmed the "retries after a failed fetch" test fails against it, restored the fix, confirmed green), then confirmed the fix. `loadSearch()` now clears `cached` before rethrowing on failure, so a second call (e.g. a "Try again" click) retries the network instead of replaying the same rejection.
- [W1] → orchestrator: applied the `course.yaml` leak-list corrections you relayed — Stage s0 retitled "Orientation" (was "Orientation and tooling") with a setup-free goal; M0.7 retitled "Online judges: WMOJ and DMOJ" (was "...the CCC Grader and DMOJ"). Also caught and fixed one I'd missed on the same sweep: M7.13 was titled "LCA and binary lifting on trees (bridge to CCO)", copied verbatim from the recon table despite my own file header saying CCO wording must not be copied — retitled to just "LCA and binary lifting on trees". Re-scanned every stage/module title and goal for score/medal/placement/setup wording; nothing else found.
- [W1] → [W2]: fixture course/lesson/module routes aren't written yet (still ahead of me: gates + `tests/fixtures/content/`). Will tell you here the moment there's a rendered fixture lesson to screenshot.
- [W1] → [W3]: applied — `biome.json`'s four `content/**`/`tests/fixtures/**` ignores replaced with `"!**/*.trace.json"` and `"!**/*.frames.json"`, so `app/dev/viz/**` is covered too. Confirmed valid JSON; full `biome check` still blocked on the in-flight `lib/viz/geometry.ts` parse errors from earlier, not this change.
- [W1] → orchestrator: `content:check` (G-SCHEMA, G-LINK-FMT, G-LINK-REF, G-LINK-PREF, G-LINK-LIVE, G-PREREQ) is written and wired (`npm run content:check`, TS via `tools/viz/register.mjs`, reused rather than reimplemented — thanks W3). First real run caught two genuine forward-reference bugs in `course.yaml` (M6.7→M6.11 and M7.1→M7.4, both verbatim from the recon table's prereq lists, which don't follow teaching order) — fixed by reordering within their stages; `content:check` is clean now. Registry verification also done: `tools/links/verify-judges.mjs` ran for real, 56/56 WMOJ problems `ok` (one title fix along the way: `ccc22s2` "Group Work" → "Good Groups", the judge's actual title), 63 DMOJ recorded `unverified`/`manual` pending the Manager's checklist (`work/04-app/dmoj-checklist.html`, `work/04-app/wmoj-verification-run-1.md`). Still ahead: `content:status`, `tools/pycheck` (G-PY-38/G-PY-RUN), `tools/style`, `tools/lint` (G-R14), G-SPELL/G-MDX wiring, gate fixtures, fixture course content.
- [W1] → orchestrator: `content:status` (ledger, `work/04-app/ledger.generated.md`, never hand-edited), `prebuild` (G-SCHEMA+G-LINK-FMT only, no Python, wired into `npm run build` via npm's own pre-script convention) and `tools/pycheck` (G-PY-38: ruff+vermin+`pypy38 -m py_compile` over every file-based example and every fenced ```python block, `bad38` must fail at least one; G-PY-RUN: byte-for-byte `.out`/`.err` reproduction under PyPy 3.8; `gen:outputs -- --scope`) are all written and wired into `verify:fast`. Every new gate has a `--root=`-isolated fixture under `tests/fixtures/gates/<gate-id>/` plus a Vitest test proving it fails there and the real tree stays clean (`tests/unit/gates/content-check.test.ts`, `check-python.test.ts` — 10 tests total, all green). `verify:full` (including `next build`) is green end to end right now.
- [W1] → [W3]: `npm run lint` currently fails on one file — `tools/viz/check.ts` has unformatted blocks (Biome would reformat ~6 spots, e.g. around lines 69-90, 117-134, 142-165, 181). Format-only, not a logic issue, and it's your file so I didn't touch it (`biome check . --write` from `main-app/` fixes it in one pass). Everything else is clean.
- [W1] → [W3]: `npm run lint` is clean now (your `tools/viz/check.ts` formatting landed, and `lib/viz/schema.ts:618` too — thanks). `npm run typecheck` has one real error, both in your `tests/unit/viz/remark-figure-numbers.test.ts`: line 20 and line 36, `TS2345`/`TS2322`, a test fixture object literal isn't assignable to `MdNode` because its `children: unknown[]` doesn't narrow to `MdNode[]`. Not my file, didn't touch it — likely just needs the fixture's `children` typed/cast to `MdNode[]` (or the nested node literals typed as `MdNode` before being placed in a `children` array).
- [W1] → orchestrator: all of batch 2's gates are now written and green: `content:check` (G-SCHEMA/G-LINK-FMT/G-LINK-REF/G-LINK-PREF/G-LINK-LIVE/G-PREREQ), `content:status`, `check:python` (G-PY-38/G-PY-RUN, incl. `gen:outputs`), `style:check` (G-STYLE, wired to `.claude/skills/avoid-ai-writing/detector/patterns.js` per D-040, `tools/style/thresholds.json` marked P4-default/P5-recalibrates), `lint:r14` (G-R14, `tools/lint/r14-banned.json` with the plan's exact patterns + a reviewed allow-list), `spell` (G-SPELL, cspell scoped to `content/**`+`tests/fixtures/content/**`+`*.md`, domain dictionary extended with real registry/design vocabulary — one real fix along the way, so it wasn't just a dictionary stuff-in: verifying a flagged word against the registry caught nothing wrong, all genuine terms), `lint:mdx` (G-MDX, remark-lint, already clean). Every gate has a `--root=`-isolated fixture under `tests/fixtures/gates/<gate-id>/` and a Vitest test proving fail-then-pass (now 12 gate-fixture tests total). All wired into `verify:fast`/`verify:full`. Next: fixture course content in `tests/fixtures/content/` (batch 2 item 6), which is the last big piece.
- [W1] → [W3]: thank you — I've started the fixture course (`tests/fixtures/content/`, stage `fx`, modules `M90.1`-`M90.3`) and see you're already filling in the `{/* W3: visual here */}` placeholders I left (M90.2's grid BFS `StepThrough`, M90.1's `CodeTrace` on `loops_and_lists.py` — both landed clean, typecheck's green). One more module still has open placeholders you might not have gotten to yet: `M90.3-fixture-visuals-and-formulas/lessons/visuals.mdx` wants a `Diagram` plus one fixture visual per remaining panel type (ArrayViz, GraphViz, TreeViz, TableViz, LineViz, PlotViz) — brief §5's "one module per remaining visualizer" ask, consolidated into one lesson for scope. No rush on all seven if that's too much for this pass; whatever you land I'll wire up / verify against the gates.
- [W1] → [W2]: the fixture course now renders — `/learn/fx/M90.1`, `/learn/fx/M90.2`, `/learn/fx/M90.3` and their lessons (e.g. `/learn/fx/M90.1/components-one`) all build. `next build` is green with real fixture pages in the static output. Worth a look/screenshot whenever you have a moment — flag anything that looks off and I'll fix the data side.
- [W1] → [W3]: all 8 panel visualizers plus the Scene landed in `M90.3` — `check:viz` reports 10 visuals, 0 problems, added `check:viz` to `verify:fast` as `G-VIZ` per your request. Caught and fixed two of my own gate scripts walking `visuals/*.viz.py` as if it were shown teaching code (a real `ruff` finding in `dijkstra.viz.py` started failing `check:python`, since your recorder scripts aren't held to the "shown on the grader" bar `.viz.py` is authoring tooling, checked by your own `check:viz`) — `check-python.ts`, `gen-outputs.ts` and `content-check.ts`'s G-PREREQ AST section now all skip `visuals/` directories. Thank you for moving fast on the fixture visuals, genuinely excellent collaboration.
- [W1] → orchestrator: batch 2 items 1-6 done; `verify:fast`/`verify:full` green end to end, including a full `next build` with the fixture course's 7 SSG pages live under `/learn/fx/*`. Item 7 (route wiring) confirmed via the build — every brief §4.10 route exists and builds, `/dev/design`+`/dev/viz` both call the shared production gate. Not done: "add `/dev/viz`+`/dev/design` to the E2E smoke list and the production-mode 404 check" — `tests/e2e/smoke.spec.ts` is a single stale test (still asserts batch-1 placeholder homepage copy against W2's real one) and a real production-mode 404 check needs its own build+server run, which reads as E2E-suite-scale (batch 3) work rather than a quick add-on; flagging instead of rushing it. Full detail in `work/04-app/_working/app-notes.md`.
